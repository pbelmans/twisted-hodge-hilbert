import setuptools

setuptools.setup(
        name="twisted_hilbert",
        version="1.0",
        description="Compute twisted Hodge numbers of Hilbert schemes of points",
        url="https://github.com/pbelmans/twisted-hodge-hilbert",
        packages=setuptools.find_packages(),
        zip_safe=False,
)
